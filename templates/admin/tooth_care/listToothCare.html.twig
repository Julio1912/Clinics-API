{% extends 'admin/base.html.twig' %}

{% block title %}Hello PositionController!{% endblock %}

{% block body %}
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{path('dashboard')}}">Dashboard</a></li>
                    <li class="breadcrumb-item active">listes des activités dentistes</li>
                </ol>
            </div>
            <div class="col-sm-6">
                <span class="breadcrumb float-sm-right">
                    <a href="{{path('toothcare.add')}}" class="float-right btn btn-primary">Ajouter nouveau activité dentiste</a>
                </span>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<div class="card ">
    <div class="card-header">
        <h3 class="card-title">
            <center>Listes des activités dentiste</center>
        </h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <table id="listToothCares" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Statut</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
            {% for toothCare in toothCares %}
                <tr>
                    <td>{{toothCare.name}}</td>
                    <td>{{ include('admin/components/block/status.html.twig', { 'etat_status': toothCare.status}) }}</td>
                    <td>
                            <a href="{{path('toothcare.edit' , {"id":toothCare.id})}}" class="btn btn-rounded btn-success" data-toggle="tooltip" data-placement="bottom">
									<i class="m-r-10 fas fa-edit"></i>
								</a>
                        </td>
                </tr>
            {% endfor %}
                
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>
<!-- /.card -->
{% endblock %}
{% block javascripts %}
{{ include('admin/components/block/blockjs.datatable.html.twig', { 'table_id': 'listToothCares', 'targets': 1 }) }}
{# {{ include('admin/block/blockjs.datatable.html.twig', { 'table_id': 'listPositions', 'targets': 4 }) }} #}
{% endblock %}